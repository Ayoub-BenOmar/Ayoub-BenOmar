#include <stdio.h>

int main(){

       int num;

       printf("Entrer les elements de tableau : ");
       scanf("%d", &num);

       int tableaux[num];
       
           for (int i = 0; i < num; i++)
           {
            /* code */
           }
           
       return 0;
    }