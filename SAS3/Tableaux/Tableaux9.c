#include <stdio.h>

int main(){

    int num;

        printf("Entrer le nombre des elements de tableau : ");
        scanf("%d", &num);
    int tableau[num];

    for (int i = 0; i < num; i++){
        printf("Entrer les elements de tableau : ");
        scanf("%d", &tableau[i]);
       }
    for (int i = 0; i < num; i++)
    {
        /* code */
    }
    

      return 0;
    }